namespace Thiccdal.Infrastructure.Remotes;

/// <summary>
/// Represents a platform target that can accept a stream relay.
/// The stream-specific contract is intentionally empty until streaming fan-out is implemented.
/// </summary>
public interface IStreamTarget
{
}
